x-base: &base
  build: .
  working_dir: /workspace
  ipc: host
  shm_size: "16gb"
  environment:
    HF_TOKEN: ${HF_TOKEN:-}
  volumes:
    - ./:/workspace
    - ${HF_HOME:-~/.cache/huggingface}:/root/.cache/huggingface
  deploy:
    resources:
      reservations:
        devices:
          - driver: nvidia
            count: all
            capabilities: [gpu]

services:
  train:
    <<: *base
    command: ["train", "--hackathon", "${HACKATHON:-}", "--epochs", "${EPOCHS:-3}", "--batch-size", "${BATCH_SIZE:-2}", "--gradient-accumulation-steps", "${GRAD_ACCUM:-4}"]

  evaluate:
    <<: *base
    command: ["evaluate", "${ADAPTER:-./hackathon_judge_lora}"]
